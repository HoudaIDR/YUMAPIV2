<UserControl x:Class="YUMAPI.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="420"
             Background="#111111">

    <UserControl.Resources>
        <Storyboard x:Key="MicroPulse" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="BoutonMicro"
                             Storyboard.TargetProperty="Opacity"
                             From="0.5" To="1.0" Duration="0:0:0.5"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#1A1A1A"
                BorderBrush="#2A2A2A" BorderThickness="0,0,0,1">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#FF6B35"
                        Width="44" Height="44" CornerRadius="22">
                    <TextBlock Text="🤖" FontSize="22"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Text="Yum! Assistant"
                               Foreground="White" FontSize="15" FontWeight="Bold"/>
                    <TextBlock x:Name="TxtStatut" Text="● En ligne"
                               Foreground="#4CAF50" FontSize="11"/>
                </StackPanel>

                <Border Grid.Column="2" Background="#222222"
                        Width="36" Height="36" CornerRadius="10"
                        Cursor="Hand" MouseDown="BtnFermer_Click">
                    <TextBlock Text="✕" Foreground="#999999"
                               FontSize="14" FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- ZONE MESSAGES -->
        <ScrollViewer Grid.Row="1" x:Name="ScrollMessages"
                      VerticalScrollBarVisibility="Auto"
                      Background="#111111" Padding="12,12,12,0">
            <StackPanel x:Name="PanneauMessages"/>
        </ScrollViewer>

        <!-- ZONE SAISIE -->
        <Border Grid.Row="2" Background="#1A1A1A"
                BorderBrush="#2A2A2A" BorderThickness="0,1,0,0"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Zone texte -->
                <Border Grid.Column="0" Background="#222222"
                        CornerRadius="24" BorderBrush="#333333"
                        BorderThickness="1" Margin="0,0,10,0">
                    <TextBox x:Name="InputMessage"
                             Background="Transparent" Foreground="White"
                             BorderThickness="0" FontSize="13"
                             Padding="16,12" CaretBrush="White"
                             TextWrapping="Wrap" MaxHeight="80"
                             VerticalScrollBarVisibility="Auto"
                             KeyDown="Input_KeyDown">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Resources>
                                    <VisualBrush x:Key="HintBrush" TileMode="None"
                                                 Opacity="0.4" Stretch="None" AlignmentX="Left">
                                        <VisualBrush.Visual>
                                            <TextBlock Text="Écrivez ou parlez..."
                                                       Foreground="Gray" FontSize="13"
                                                       Margin="16,12,0,0"/>
                                        </VisualBrush.Visual>
                                    </VisualBrush>
                                </Style.Resources>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background" Value="{StaticResource HintBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Border>

                <!-- 🎤 Bouton micro -->
                <Border x:Name="BoutonMicro"
                        Grid.Column="1"
                        Background="#222222" Width="46" Height="46"
                        CornerRadius="23" Cursor="Hand"
                        Margin="0,0,8,0"
                        ToolTip="Appuyer pour parler"
                        MouseDown="BtnMicro_Click">
                    <TextBlock x:Name="IconeMicro"
                               Text="🎤" FontSize="20"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <!-- ➤ Bouton envoyer -->
                <Border Grid.Column="2"
                        Background="#FF6B35" Width="46" Height="46"
                        CornerRadius="23" Cursor="Hand"
                        MouseDown="BtnEnvoyer_Click">
                    <TextBlock Text="➤" Foreground="White" FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="2,0,0,0"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
